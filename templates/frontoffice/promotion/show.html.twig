{% extends 'base.html.twig' %}

{% block title %}Promotion{% endblock %}

{% block body %}
    <h1>Promotion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promotion.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ promotion.name }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ promotion.year }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_frontoffice_promotion_index') }}">back to list</a>

    <a href="{{ path('app_frontoffice_promotion_edit', {'id': promotion.id}) }}">edit</a>

    {{ include('frontoffice/promotion/_delete_form.html.twig') }}
{% endblock %}
