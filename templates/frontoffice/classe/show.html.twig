{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ classe.start ? classe.start|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ classe.end ? classe.end|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_frontoffice_classe_index') }}">back to list</a>

    <a href="{{ path('app_frontoffice_classe_edit', {'id': classe.id}) }}">edit</a>

    {{ include('frontoffice/classe/_delete_form.html.twig') }}
{% endblock %}
