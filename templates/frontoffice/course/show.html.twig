{% extends 'base.html.twig' %}

{% block title %}Course{% endblock %}

{% block body %}
    <h1>Course</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ course.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_frontoffice_course_index') }}">back to list</a>

    <a href="{{ path('app_frontoffice_course_edit', {'id': course.id}) }}">edit</a>

    {{ include('frontoffice/course/_delete_form.html.twig') }}
{% endblock %}
