{% extends 'base.html.twig' %}

{% block title %}Student{% endblock %}

{% block body %}
    <h1>Student</h1>

    <table class="table">
        <tbody>
        <tr>
            <td>{{ student.id }}</td>
            <td>{{ student.user.fullname }}</td>
            <td>{{ student.promotion.name }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_frontoffice_student_index') }}">back to list</a>

    <a href="{{ path('app_frontoffice_student_edit', {'id': student.id}) }}">edit</a>

    {{ include('frontoffice/student/_delete_form.html.twig') }}
{% endblock %}
